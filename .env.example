# ==========================================================
# Property Management System - Environment Configuration
# ==========================================================
# This file contains all environment variables needed for the
# application. Copy this file to .env and fill in your values.
#
# DO NOT COMMIT .env FILES WITH REAL CREDENTIALS TO VERSION CONTROL!
# ==========================================================

# ==========================================================
# AWS Cognito Authentication Configuration
# ==========================================================

# AWS Region for general services
PROPMGMT_AWS_REGION=us-west-1

# Cognito User Pool Configuration
PROPMGMT_COGNITO_REGION=us-west-1
PROPMGMT_COGNITO_USER_POOL_ID=us-west-1_XXXXXXXXX
PROPMGMT_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
PROPMGMT_COGNITO_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PROPMGMT_COGNITO_DOMAIN=your-domain.auth.region.amazoncognito.com
PROPMGMT_COGNITO_SCOPES=openid email profile

# ==========================================================
# Application URLs and Endpoints
# ==========================================================

# Backend API URL (where FastAPI is running)
PROPMGMT_API_URL=http://localhost:8000

# Frontend URL (where React app is running)
PROPMGMT_FRONTEND_URL=http://localhost:3000

# OAuth callback URL (must match Cognito app client setting)
PROPMGMT_REDIRECT_URI=http://localhost:8000/auth/callback

# ==========================================================
# Security Settings
# ==========================================================

# Set to true in production with HTTPS
PROPMGMT_COOKIE_SECURE=false

# ==========================================================
# React Frontend Environment Variables
# ==========================================================
# These variables will be used by the React frontend
# They must be prefixed with REACT_APP_ to be accessible in React

# Cognito configuration for frontend
REACT_APP_COGNITO_REGION=${PROPMGMT_COGNITO_REGION}
REACT_APP_COGNITO_USER_POOL_ID=${PROPMGMT_COGNITO_USER_POOL_ID}
REACT_APP_COGNITO_APP_CLIENT_ID=${PROPMGMT_COGNITO_CLIENT_ID}
REACT_APP_COGNITO_DOMAIN=${PROPMGMT_COGNITO_DOMAIN}
REACT_APP_REDIRECT_SIGN_IN=${PROPMGMT_REDIRECT_URI}
REACT_APP_REDIRECT_SIGN_OUT=${PROPMGMT_API_URL}/auth/logout
REACT_APP_API_URL=${PROPMGMT_API_URL}

# ==========================================================
# Application Settings
# ==========================================================

# Application mode (development or production)
APP_ENV=development

# Debug mode (true/false)
PROPMGMT_DEBUG=true

# ==========================================================
# Database Configuration (if needed)
# ==========================================================

# Uncomment and configure these if your application uses a database
# DATABASE_URL=postgresql://user:password@localhost/dbname
# DATABASE_POOL_SIZE=5
# DATABASE_MAX_OVERFLOW=10
